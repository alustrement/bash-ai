{
  "name": "@alustrement/bash-ai",
  "version": "1.0.0",
  "main": "bin/bash-ai",
  "author": "aurelien lustrement <[aurelien.lustrement@live.fr]>",
  "bin": {
    "??": "bin/bash-ai"
  },
  "license": "MIT",
  "scripts": {
    "start": "ts-node src/app.ts",
    "dev": "ts-node-dev --respawn src/app.ts",
    "clean": "tsc --build --clean",
    "build": "tsc",
    "preinstall": "ls -l | grep 'node_modules' && echo 'preinstall'",
    "postinstall": "npm run clean && npm run build && echo 'Installed.'",
    "prepare": "npm run clean && npm run build && echo 'Prepared.'",
    "prepublishOnly": "npm run clean && npm run build ",
    "preversion": "npm run clean && npm run build",
    "version": "npm run clean && npm run build",
    "postversion": "git push && git push --tags"
  },
  "exports": {
    ".": {
      "import": "./dist/app.js",
      "require": "./dist/app.js"
    },
    "./package.json": "./package.json"
  },
  "dependencies": {
    "@types/node": "^18.15.11",
    "commander": "^14.0.0",
    "openai": "^5.19.1"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  },
  "description": "Bash AI is a command-line tool that uses the power of OpenAI's GPT to provide intelligent suggestions and automation for your bash commands.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alustrement/bash-ai.git"
  },
  "keywords": [
    "gpt",
    "ai",
    "open-ai"
  ],
  "bugs": {
    "url": "https://github.com/alustrement/bash-ai/issues"
  },
  "homepage": "https://github.com/alustrement/bash-ai#readme",
  "release": {
    "branches": [
      "main"
    ]
  },
  "publishConfig": {
    "access": "public"
  }
}
